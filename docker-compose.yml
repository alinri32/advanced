
services:
  backend:
    build: .
    container_name: backend
    command: python manage.py runserver 0.0.0.0:80
    volumes:
      - ./core:/app
    ports:
      - "80:80"
    environment:
      - SECRET_KEY=test
      - DEBUG=True